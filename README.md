# 🥔 FastAPI Potato Pet Project

Простое API на FastAPI с автоматическим развертыванием через Docker. 

## 🚀 Как запустить

Для запуска вам понадобится только установленный **Docker Desktop**.

1. **Клонируйте репозиторий:**
   ```bash
   git clone [https://github.com/glfess/FastAPIpotato-pet-project.git](https://github.com/glfess/FastAPIpotato-pet-project.git)
   cd FastAPIpotato-pet-project
Запустите контейнеры:

Bash
\```bash
    docker-compose up --build
\```
Готово! * API доступно по адресу: http://localhost:8000

Интерактивная документация (Swagger): http://localhost:8000/docs

🛠 Технологии
FastAPI — основной фреймворк.

PostgreSQL 18 — база данных.

SQLAlchemy / Alembic — работа с БД и миграциями.

Docker / Docker Compose — контейнеризация.

📦 Структура проекта
app/ — исходный код приложения.

docker-compose.yml — конфигурация сервисов (приложение + БД).

Dockerfile — инструкция по сборке образа.

requirements.txt — зависимости Python.

🔧 Решение проблем (Troubleshooting)
Если во время сборки возникают ошибки сети (например, при установке пакетов), попробуйте:

Отключить VPN.

Использовать команду прямой сборки:

Bash
```bash
docker build --network=host -t fastapi_app .
